{
  "$schema": "https://raw.githubusercontent.com/ow-mods/owml/master/schemas/config_schema.json",
  "enabled": true,
  "settings": {
    "allowSpawnOverride": {
      "type": "toggle",
      "title": "Allow Spawn Override",
      "tooltip": "Let jam entries change the spawn point. You have to restart or reload configs for changing this to take effect.",
      "value": false
    },
    "showAllowedVolume": {
      "type": "toggle",
      "title": "Show Allowed Volume",
      "tooltip": "Toggle to show a sphere outlining the volume that jam entries are allowed.",
      "value": false
    }
  }
}
